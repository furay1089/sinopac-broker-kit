# 系統保護聲明

## 此套件的範圍

`sinopac-broker-kit` 是從主操盤術 AI 交易系統萃取出的**共用基礎層**，包含：
- 永豐金 Shioaji API 整合
- Telegram Bot 設置工具
- 設定檔範本

## 主系統不可被外部存取或修改

### 保護原則

1. **主系統目錄（`ai_trading/`）不屬於本套件的範圍**
   - 本套件不包含、不參照、不修改主系統的任何核心邏輯
   - 主系統的操盤術邏輯、決策管道、交易規則是獨立且封閉的

2. **讀取主系統狀態的唯一合法方式**
   - 透過 `state.json` 檔案**唯讀**存取
   - 掛載時必須加 `:ro`（read-only）
   - 禁止任何寫入、修改、刪除操作

3. **禁止行為**
   - 禁止從外部系統呼叫主系統的任何內部函式
   - 禁止修改主系統的 `engine/`、`scheduler/`、`tg_bot/` 目錄
   - 禁止覆蓋主系統的設定檔（`state.json`、`trade_config.json`）
   - 禁止在主系統容器內執行額外程序

4. **整合的正確方式**
   - 你的系統讀取 `state.json`（只讀）取得主系統信號
   - 衝突判斷邏輯寫在**你自己的系統**內
   - 你的系統獨立維護自己的狀態和設定檔

## 為什麼這樣設計

主操盤術系統的核心是多年調校的交易規則與四套操盤術邏輯，
對主系統的任何未授權修改都可能導致下單錯誤、停損失效或資金損失。

透過「共用層獨立套件化 + 主系統只讀」的架構，可以：
- 讓多個系統復用同一套下單基礎設施
- 確保主系統的邏輯不被意外污染
- 當共用層有 bug 時，修復影響所有系統，但不碰核心邏輯

---

**任何對主系統的修改請求，必須由主系統的開發者明確授權。**
